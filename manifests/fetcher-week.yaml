apiVersion: apps/v1
kind: Deployment
metadata:
  name: fetcher-week
  namespace: earthquakes-app
  labels:
    run: fetcher-week
spec:
  replicas: 1
  selector:
    matchLabels:
      run: fetcher-week

  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate

  template:
    metadata:
      labels:
        run: fetcher-week
    spec:
      nodeSelector:
        kubernetes.io/hostname: arboretum
      containers:
        - image: localhost:5000/fetcher:latest
          imagePullPolicy: Always
          name: fetcher
          env:
            - name: NATS_URL
              value: "nats://nats:4222"
            - name: POLL_FREQUENCY
              value: "8h" # 30m, 1h, 8h etc.
            - name: FEED_TYPE
              value: "all_week"
            - name: FEEDS_SUBJECT
              value: "earthquakes.raw"
            - name: IMPACT_SUBJECT
              value: "earthquakes.impact"
          resources:
            limits:
              cpu: "100m"
              memory: "64Mi"
            requests:
              cpu: "50m"
              memory: "32Mi"
      restartPolicy: Always
