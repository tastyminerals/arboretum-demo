apiVersion: v1
kind: Service
metadata:
  name: postgres-service
  namespace: earthquakes-app
  labels:
    app: postgres-service
spec:
  type: ClusterIP
  selector:
    app: postgres-sts
  ports:
    - protocol: TCP
      port: 5432
      targetPort: postgres-port
